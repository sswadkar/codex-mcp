{
  "name": "codex-mcp",
  "private": true,
  "version": "0.1.0",
  "description": "Local MCP servers for host-integrated tools",
  "workspaces": [
    "packages/*",
    "servers/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r build && pnpm -r typecheck",
    "test": "pnpm -r build && pnpm -r test"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
